from setuptools import setup

setup(
    name='GreedyFHist',
    version='0.1.0',
    py_modules=['main'],
    install_requires=[
        'Click',
    ],
    entry_points={
        'console_scripts': [
            'greedyfhist = main:cli'
            # 'register = main:register',
            # 'warp = main:warp'
        ],
    },
)